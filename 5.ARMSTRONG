#include<stdio.h>
int main()
{
	int n,rem,sum=0,temp;
	printf("enter:");
	scanf("%d",&n);
	temp=n;
	while(n>0)
	{
		rem=n%10;
		sum=sum+(rem*rem*rem);
		n=n/10;
	}
	if(sum==temp)
	printf("armstrong");
	else
	printf("not a armstrong");
	return 0;
}
