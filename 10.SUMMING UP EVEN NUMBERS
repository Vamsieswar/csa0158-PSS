#include<stdio.h>
int main()
{
	int n,i,sum=0;
	printf("enter the max no:");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		if(i%2==0)
		{
			printf("%d ",i);
			sum=sum+i;
		}
	}
	printf("\nsum of even nos are:%d",sum);
	return 0;
}
